# Discord メール通知Bot 引継ぎ資料

> **この資料について**
> 事前知識がない方でも、この資料だけでセットアップ・運用・トラブル対応ができるよう作成しています。
> 上から順に読み進めてください。

---

## 目次

1. [このBotが何をするのか](#1-このbotが何をするのか)
2. [システム全体の構成](#2-システム全体の構成)
3. [ファイル構成と役割](#3-ファイル構成と役割)
4. [必要なアカウント・サービス一覧](#4-必要なアカウントサービス一覧)
5. [環境変数・設定一覧](#5-環境変数設定一覧)
6. [初回セットアップ手順](#6-初回セットアップ手順)
7. [動作フロー](#7-動作フロー)
8. [GASトリガーの設定](#8-gasトリガーの設定)
9. [日常運用](#9-日常運用)
10. [トラブルシューティング](#10-トラブルシューティング)
11. [セキュリティ](#11-セキュリティ)
12. [実装上の重要ポイント](#12-実装上の重要ポイント)

---

## 1. このBotが何をするのか

Gmailに外部からメールが届いたとき、**自動でDiscordに通知**し、チームで対応状況を管理できるBotです。

### できること

| 機能 | 説明 |
|---|---|
| **メール自動通知** | 新着メールをDiscordのフォーラムチャンネルにスレッドとして投稿する |
| **対応状況の管理** | ボタンを押すだけで「未対応 → 対応中 → 完了」と状態を切り替えられる |
| **メンション通知** | メールの件名・本文・送信者をキーワードで判定し、担当者をメンションする |
| **日次サマリー** | 毎朝、未対応・対応中の件数をリマインドチャンネルに投稿する |
| **エラー通知** | 送信失敗時にDiscord Webhookで即時アラートを飛ばす |

### Discordでの見た目

```
【メール通知チャンネル（フォーラム）】
┌─────────────────────────────────┐
│ 【未対応】○○株式会社からのお問い合わせ  │
│ @担当者 新着メールの通知です           │
│ 送信者: example@example.com         │
│ 文頭: お世話になっております...        │
│ [対応開始] [対応完了]               │
└─────────────────────────────────┘

【リマインドチャンネル（毎朝投稿）】
┌─────────────────────────────────┐
│ 📊 4月28日(火)の対応状況サマリー      │
│ 🔴 未対応: 3件                      │
│ 🟡 対応中: 1件                      │
└─────────────────────────────────┘
```

---

## 2. システム全体の構成

```
Gmail受信トレイ
    │
    │ GAS（Google Apps Script）が数分おきに自動チェック
    ▼
Cloudflare Worker（HMAC認証で不正アクセスを防御）
    │
    │ Discord Bot Token を使ってDiscord APIを呼び出す
    ▼
Discordフォーラムチャンネル
    └── スレッド作成・ボタンによるステータス管理
    └── リマインドチャンネルへの日次サマリー投稿
```

### 各サービスの役割

| サービス | 役割 |
|---|---|
| **Gmail** | メールの受信元 |
| **Google スプレッドシート** | キーワードとメンション先の対応表、設定値の保管場所 |
| **Google Apps Script (GAS)** | Gmailを定期チェックしてCloudflare Workerに通知を送るスクリプト |
| **Cloudflare Worker** | GASからのリクエストを受け取り、Discord APIを呼び出すサーバーレス関数 |
| **Discord** | 通知の表示・ボタン操作による対応状況の管理 |

---

## 3. ファイル構成と役割

| ファイル名 | 配置場所 | 役割 |
|---|---|---|
| `worker.js` | Cloudflare Worker | メール通知の受け取り・Discordへの投稿・ボタン操作の処理・日次サマリーの集計と投稿 |
| `gas.js` | Google Apps Script | Gmailの定期チェック・Workerへの通知送信・日次サマリーのトリガー |

---

## 4. 必要なアカウント・サービス一覧

セットアップには以下のアカウントが必要です。すべて無料プランで動作します。

| サービス | 用途 | URL |
|---|---|---|
| Googleアカウント | Gmail・GAS・スプレッドシートの利用 | https://google.com |
| Cloudflareアカウント | Workerのデプロイ | https://cloudflare.com |
| Discordアカウント | Bot作成・サーバー管理 | https://discord.com |
| Discord Developer Portal | Botの作成・公開鍵の取得 | https://discord.com/developers |

---

## 5. 環境変数・設定一覧

### 5-1. Cloudflare Worker の環境変数

Cloudflare Dashboard → Workers → 対象のWorker → **Settings → Variables and Secrets** で設定します。

| 変数名 | 内容 | 取得方法 |
|---|---|---|
| `PUBLIC_KEY` | Discord Botの公開鍵 | Developer Portal → アプリ → General Information → Public Key |
| `BOT_TOKEN` | Discord Botのトークン | Developer Portal → アプリ → Bot → Token（⚠️絶対に外部に漏らさない） |
| `CHANNEL_ID` | メール通知用フォーラムチャンネルのID | Discordで開発者モードをオンにしてチャンネルを右クリック → IDをコピー |
| `GUILD_ID` | DiscordサーバーのID | Discordでサーバー名を右クリック → IDをコピー |
| `TAG_UNRESOLVED` | 【未対応】タグのID | フォーラムチャンネルの設定 → タグを右クリック → IDをコピー |
| `TAG_PROGRESS` | 【対応中】タグのID | 同上 |
| `TAG_COMPLETED` | 【完了】タグのID | 同上 |
| `GAS_SECRET` | HMAC署名用の共有シークレット | 任意の長い文字列を自分で決める（例: パスワードマネージャーで生成） |
| `SUMMARY_CHANNEL_ID` | 日次サマリー投稿先チャンネルのID | リマインドチャンネルを右クリック → IDをコピー |

> **⚠️ 注意**
> `BOT_TOKEN` と `GAS_SECRET` は絶対に他人に教えないでください。
> 漏洩した場合はすぐに再発行してください（手順は [セキュリティ](#11-セキュリティ) を参照）。

### 5-2. GAS スクリプトプロパティ

GASエディタ → 歯車アイコン（プロジェクトの設定）→ **スクリプトプロパティ** で設定します。

| プロパティ名 | 内容 |
|---|---|
| `CLOUDFLARE_URL` | WorkerのエンドポイントURL（Cloudflare Dashboard → Workers → 対象Worker → URLをコピー） |
| `SPREADSHEET_ID` | スプレッドシートのID（スプレッドシートのURLの `/d/` と `/edit` の間の文字列） |
| `GAS_SECRET` | Workerの `GAS_SECRET` と**完全に同じ文字列** |
| `ERROR_WEBHOOK_URL` | エラー通知先のDiscord Webhook URL（任意・未設定でも動作する） |

### 5-3. スプレッドシート「設定」シート

| セル | 内容 | 備考 |
|---|---|---|
| B1 | 最終メッセージID | GASが自動更新するため手動変更不要（初回は空でOK） |
| B2 | 自分のメールアドレス | 自分が送ったメールを通知から除外するために使用 |
| B3 | デフォルトメンション先 | キーワードが一致しなかった場合のメンション先。例: `<@123456789>` または `@here` |

### 5-4. スプレッドシート「管理シート」

キーワードと通知先Discordユーザーの対応表です。2行目以降にデータを入力します。

| 列 | 内容 | 例 |
|---|---|---|
| A列 | キーワード（件名・本文・送信者でマッチング） | `請求書`、`example.com` |
| C列 | メンション先のDiscord ID | `123456789`（数字のみ）または `<@123456789>` |
| D列 | 投稿先の専用チャンネルID（空欄の場合はデフォルトチャンネルに投稿） | `1526086972282769551` |
| E列 | デフォルトチャンネルにも重複投稿するか（チェックボックス）。**D列に専用チャンネルIDが設定されている行でのみ意味を持つ**。空欄/`TRUE`＝専用チャンネル＋デフォルトチャンネルの両方に投稿（従来通り）、`FALSE`＝専用チャンネルのみに投稿 | チェックボックス |

> **Discord IDの確認方法**
> Discordで開発者モードをオンにして、メンションしたいユーザー名を右クリック → **IDをコピー**

> **E列（重複投稿チェックボックス）の設定方法**
> Google Sheetsで対象範囲（例: E2:E100）を選択 →「挿入」→「チェックボックス」（または「データ」→「データの入力規則」で条件を「チェックボックス」に設定）。
> **移行時の注意**: 空欄の行は「重複する（TRUE相当）」として扱われるため、既存のD列付き行の挙動は変わりません。重複を止めたい行（例: 寄附）だけ、明示的にチェックを外して未チェック（FALSE）にしてください。

### 5-5. スプレッドシート「ステータス管理」「ステータス管理_アーカイブ」シート

メールの返信要否を自動判定して記録するシートです。どちらもGASが未作成なら自動生成するため、手動でシートを用意する必要はありません。

**「ステータス管理」**（現在返信が必要なもの・直近解決したものを表示）

| 列 | 内容 |
|---|---|
| A | スレッドID |
| B | 件名 |
| C | 送信者 |
| D | 検知日時 |
| E | 返信要否（`要返信` / `返信済み`） |
| F | 最終チェック日時 |

- **判定基準**: スレッドの最新メッセージの送信者が設定シートB2のアドレスなら「返信済み」、それ以外なら「要返信」
- 一度「返信済み」になったスレッドに新着メールが来た場合、同じ行が「要返信」に戻る（行が増えることはない）
- 「返信済み」になってから60日（`GAS.js`内の`ARCHIVE_AFTER_DAYS`定数で変更可能）経過すると、自動的に「ステータス管理_アーカイブ」シートへ移動する

**「ステータス管理_アーカイブ」**

「ステータス管理」と同じ列＋G列「アーカイブ日時」。過去の記録の参照用で、書き戻されることはない。

---

## 6. 初回セットアップ手順

### Step 1: Discord Botを作成する

1. [Discord Developer Portal](https://discord.com/developers) にアクセス
2. **New Application** → アプリ名を入力して作成
3. 左メニュー **Bot** → **Add Bot** → Botを作成
4. **Token** の **Reset Token** をクリックしてトークンをコピー（後で使用）
5. 左メニュー **General Information** → **Public Key** をコピー（後で使用）
6. 左メニュー **OAuth2 → URL Generator** → `bot` と `applications.commands` にチェック
7. Bot Permissions で以下にチェックを入れる
   - `Send Messages`、`Read Message History`、`Manage Threads`、`View Channels`
8. 生成されたURLをブラウザで開いてBotをサーバーに招待する

### Step 2: Discordチャンネルを準備する

1. **メール通知用フォーラムチャンネル**を作成
   - チャンネル設定 → タグを追加:「未対応」「対応中」「完了」の3つ
   - 各タグのIDをコピーしておく（開発者モードで右クリック → IDをコピー）
2. **リマインドチャンネル**（通常のテキストチャンネル）を作成
3. 各チャンネルのIDをコピーしておく

### Step 3: Cloudflare Workerをデプロイする

1. [Cloudflare](https://cloudflare.com) にログイン
2. **Workers & Pages** → **Create** → **Create Worker**
3. Worker名を入力して作成
4. **Edit Code** を開き、`worker.js` の内容を全てコピー&ペーストして **Deploy**
5. **Settings → Variables and Secrets** で [5-1](#5-1-cloudflare-worker-の環境変数) の環境変数を全て設定
6. WorkerのURL（`https://xxx.workers.dev`）をコピーしておく

### Step 4: Discord BotのInteraction URLを設定する

1. Developer Portal → アプリ → **General Information**
2. **Interactions Endpoint URL** にStep 3でコピーしたWorkerのURLを貼り付けて保存
   - Discordが自動的に疎通確認を行い、成功すると保存できる

### Step 5: スプレッドシートを準備する

1. Googleスプレッドシートを新規作成
2. シート名を「設定」に変更し、以下を入力:
   - B2: 自分のメールアドレス
   - B3: デフォルトのメンション先（`@here` など）
3. 新しいシートを追加してシート名を「管理シート」に変更
4. 1行目にヘッダー（任意）、2行目以降にキーワードとDiscord IDを入力

### Step 6: GASを設定する

1. [Google Apps Script](https://script.google.com) にアクセス → **新しいプロジェクト**
2. `gas.js` の内容を全てコピー&ペーストして保存
3. 歯車アイコン（プロジェクトの設定）→ **スクリプトプロパティ** に [5-2](#5-2-gas-スクリプトプロパティ) の値を全て設定

### Step 7: 動作確認

1. GASエディタで関数を `checkMailsAndNotify` に選択して **実行**
   - 初回実行: 最新メッセージIDを記録するだけ（Discordには何も来ない）
2. 別のメールアドレスからテストメールを送信
3. 再度 `checkMailsAndNotify` を実行 → Discordにスレッドが作成されるか確認
4. GASエディタで関数を `testDailySummary` に選択して **実行** → リマインドチャンネルにサマリーが届くか確認

### Step 8: トリガーを設定する

動作確認ができたら定期実行の設定を行います（[GASトリガーの設定](#8-gasトリガーの設定) を参照）。

---

## 7. 動作フロー

### メール通知フロー（自動・数分おき）

```
1. GAS が checkMailsAndNotify() を実行
2. Gmailの受信トレイから最新50スレッドを取得
3. スプレッドシートB1に保存した前回の最終メッセージIDより新しいメールを抽出
4. B2のアドレスと一致する送信元（自分のメール）は除外
5. 管理シートのキーワードとマッチングしてメンション先を決定
   └── 一致なし → B3のデフォルトメンション先を使用
6. HMAC署名（+ タイムスタンプ）を付けてCloudflare WorkerにPOST
7. WorkerがHMAC・タイムスタンプを検証
8. Discord APIでフォーラムチャンネルにスレッドを作成
9. 全件処理後にB1を最新IDで更新
```

### ボタン操作フロー（手動）

```
1. Discordのボタンを押すとDiscordがWorkerにInteractionを送信
2. WorkerがEd25519署名を検証（Discordからの正規リクエストか確認）
3. Embedのタイトル・色を即座に更新して返答
4. 500ms後に非同期でスレッドのタグ・アーカイブをPATCH更新
```

| ボタン | タグ | タイトル | 色 | アーカイブ |
|---|---|---|---|---|
| 対応開始 | 【対応中】 | 【対応中】○○ | 🟡 黄 | なし |
| 対応完了 | 【完了】 | 【完了】○○ | 🟢 緑 | あり（スレッドが閉じる） |
| 未対応に戻す | 【未対応】 | 【未対応】○○ | 🔴 赤 | なし |

### 返信要否チェックフロー（自動・メールチェックのたび）

```
1. GAS が checkMailsAndNotify() の冒頭で updateReplyStatuses() を実行
2. 「ステータス管理」シートの「要返信」行だけを対象に、スレッドを再取得
3. スレッドの最新メッセージの送信者がB2のアドレスなら「返信済み」に更新
4. 続けて archiveResolvedStatuses() を実行
5. 「返信済み」になってから60日経過した行を「ステータス管理_アーカイブ」へ移動
6. 新着メールを検知した場合は、通知処理と合わせて upsertReplyStatus() で
   「ステータス管理」シートに「要返信」として記録（同じスレッドが既にあれば上書き）
```

### 日次サマリーフロー（自動・毎朝）

```
1. GAS が sendDailySummary() を実行
2. WorkerにX-GAS-Action: summaryヘッダー付きでPOST
3. WorkerがDiscord APIでサーバー全体のアクティブスレッドを取得
4. TAG_UNRESOLVED・TAG_PROGRESSのタグが付いたスレッドを集計
5. Botとしてリマインドチャンネルにサマリーを投稿
```

---

## 8. GASトリガーの設定

GASエディタ左メニューの **時計アイコン（トリガー）** → **トリガーを追加** で設定します。

### メール定期チェック用

| 項目 | 設定値 |
|---|---|
| 実行する関数 | `checkMailsAndNotify` |
| イベントのソース | 時間主導型 |
| 時間の種類 | 分ベースのタイマー |
| 間隔 | 1〜5分おき（推奨: 5分） |

### 日次サマリー用

| 項目 | 設定値 |
|---|---|
| 実行する関数 | `sendDailySummary` |
| イベントのソース | 時間主導型 |
| 時間の種類 | 日付ベースのタイマー |
| 時刻 | 毎朝9時など任意の時間 |

---

## 9. 日常運用

### 通常時にやること

- **担当者の追加・変更**: スプレッドシートの「管理シート」のA列（キーワード）とC列（Discord ID）を編集するだけ。コードの変更は不要。
- **デフォルトメンション先の変更**: 設定シートのB3を編集するだけ。

### 定期メンテナンス（年1回推奨）

セキュリティのため、以下を更新してください。

1. **GAS_SECRET の更新**
   - 新しいランダム文字列を生成
   - Cloudflare Worker の `GAS_SECRET` を更新してデプロイ
   - GAS のスクリプトプロパティ `GAS_SECRET` も同じ値に更新
2. **BOT_TOKEN の更新**
   - Developer Portal → Bot → **Reset Token** で再発行
   - Cloudflare Worker の `BOT_TOKEN` を更新してデプロイ

### テスト用関数

| 関数名 | 用途 |
|---|---|
| `testSendToDiscord()` | テスト用のDiscord通知を送信する |
| `testDailySummary()` | 日次サマリーを即時送信してテストする |

---

## 10. トラブルシューティング

まずGASの **実行ログ**（GASエディタ → 実行数）とCloudflare Workerの **ログ**（Dashboard → Workers → ログ）を確認してください。

### よくあるエラーと対処法

#### ❌ GASログ: `Worker returned HTTP 403`

HMAC署名が一致していません。

**確認事項:**
- GASのスクリプトプロパティ `GAS_SECRET` とWorkerの環境変数 `GAS_SECRET` が**完全に同じ文字列**か確認
- コピー時に前後に空白が入っていないか確認

#### ❌ GASログ: `Unexpected error while getting the method or property newBlob on object Utilities`

GASのランタイムバージョンが古いです。

**対処法:**
GASエディタ → 歯車アイコン（プロジェクトの設定）→ ランタイムを **V8** に変更

#### ❌ Workerログ: `Discord API error [GET /guilds/.../threads/active]: 404`

`GUILD_ID` が正しくないか、BotがサーバーにいないかBotの権限不足です。

**確認事項:**
- Discordでサーバー名を右クリック → IDをコピー → Workerの `GUILD_ID` と一致するか確認
- Discordサーバーの設定 → メンバー一覧にBotが表示されているか確認
- BotのロールにViewChannel権限があるか確認

#### ❌ Workerログ: `Discord API error [POST /channels/.../threads]: 403`

BotにフォーラムチャンネルへのWrite権限がありません。

**対処法:**
フォーラムチャンネルの設定 → 権限 → BotのロールにManageThreads・SendMessagesを付与

#### ❌ Discordのボタンを押しても反応しない

WorkerのInteraction Endpoint URLの設定が古い可能性があります。

**確認事項:**
- Developer Portal → General Information → Interactions Endpoint URL が現在のWorkerのURLと一致しているか確認

#### ❌ メールが来ているのにDiscordに通知が来ない

**確認手順:**
1. GASのトリガーが正しく設定されているか確認（実行数に履歴があるか）
2. 設定シートのB2（自分のメールアドレス）が正しく入力されているか確認
3. 送信元メールアドレスがB2に含まれていて除外されていないか確認
4. GASの実行ログにエラーが出ていないか確認

---

## 11. セキュリティ

### 実装しているセキュリティ対策

| 対策 | 内容 |
|---|---|
| **HMAC-SHA256署名** | GASからWorkerへのリクエストが正規のものか検証する |
| **タイムスタンプ検証** | 5分以上古いリクエストを拒否する（リプレイ攻撃対策） |
| **定数時間比較** | HMAC比較時のタイミング攻撃を防ぐ |
| **Ed25519署名検証** | DiscordからのInteractionが正規のものか検証する |

### 漏洩時の緊急対応

#### BOT_TOKEN が漏洩した場合

1. Developer Portal → Bot → **Reset Token** でトークンを即座に無効化・再発行
2. Cloudflare Worker の `BOT_TOKEN` を新しいトークンに更新してデプロイ

#### GAS_SECRET が漏洩した場合

1. 新しいランダム文字列を生成（20文字以上推奨）
2. Cloudflare Worker の `GAS_SECRET` を更新してデプロイ
3. GAS のスクリプトプロパティ `GAS_SECRET` を同じ値に更新
4. **両方同時に更新しないと通知が止まる**ため注意

---

## 12. 実装上の重要ポイント

過去のトラブルと解決策をまとめています。コードを変更する際に参考にしてください。

### `request.text()` は一度しか読めない

Cloudflare Workerで `request.text()` を複数回呼ぶと2回目以降は空文字になります。
コード冒頭で `const bodyText = await request.text()` と一度だけ読み、以降は `bodyText` を使い回しています。

### GASのHMAC署名はUTF-8変換が必要

`Utilities.computeHmacSha256Signature()` に文字列をそのまま渡すと、Worker側と異なるHMACが生成されることがあります。
`Utilities.newBlob(text, 'text/plain', 'UTF-8').getBytes()` でバイト列に変換してから渡すことで解決しています。

### メッセージIDの比較は `>` で行う

`===` で完全一致を探す方式は、大量メール受信時に `lastMessageId` を見失うリスクがあります。
GmailのメッセージIDは新しいほど大きい値になるため、`msgId > lastMessageId` で比較しています。

### メール取得ループで `break` しない

`getInboxThreads()` はスレッドの最終更新日時でソートされるため、古いスレッドへの返信があるとスレッド順が変わります。
`break` を使わず全50件をスキャンすることで取りこぼしを防いでいます。

### サマリーのスレッド取得は `GUILD_ID` 経由

フォーラムチャンネルのIDで `GET /channels/{id}/threads/active` を叩いても **404** になります。
`GET /guilds/{guild_id}/threads/active` を使うことでサーバー全体のアクティブスレッドを取得できます。
アーカイブ済みスレッドは `GET /channels/{channel_id}/threads/archived/public` で取得しています。

### 自分のメールアドレスはセルから取得する

`Session.getActiveUser().getEmail()` はトリガー実行時に空文字を返すことがあります。
設定シートのB2に直接記載して読み込むことで解決しています。
